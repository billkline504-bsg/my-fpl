# Copy to .env.local after running `supabase start` (or `supabase status`
# if already running) from the repo root.

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=http://127.0.0.1:4000
